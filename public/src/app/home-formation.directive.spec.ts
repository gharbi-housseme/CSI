import { HomeFormationDirective } from './home-formation.directive';

describe('HomeFormationDirective', () => {
  it('should create an instance', () => {
    const directive = new HomeFormationDirective();
    expect(directive).toBeTruthy();
  });
});
